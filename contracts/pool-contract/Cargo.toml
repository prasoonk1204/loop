[package]
name = "pool-contract"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = "26.1.0"
ed25519-dalek = "=2.2.0"

[dev-dependencies]
soroban-sdk = { version = "26.1.0", features = ["testutils"] }
