[package]
name = "pool-contract"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = "27.0.0"
member-registry = { path = "../member-registry" }

[dev-dependencies]
soroban-sdk = { version = "27.0.0", features = ["testutils"] }
